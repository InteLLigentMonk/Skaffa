import { ThemedText } from "@/components/themed-text";
import { ThemedView } from "@/components/themed-view";

const Register = () => {
  return (
    <ThemedView>
      <ThemedText>Logga in dig</ThemedText>
    </ThemedView>
  );
};

export default Register;
